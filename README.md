To do: Map out which external LSP binaries need to be installed for Vscode extensions to work (NixOS)
nufmt
nu-lint

For NixOS home-manager: make ~/dotfiles folder & git this repo inside ~/dotfiles & not in ~/
